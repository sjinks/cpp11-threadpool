language: cpp

branches:
  only:
    - master

sudo: required
dist: trusty

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq -y install cxxtest

script:
  -  make CXXFLAGS="-O2 -Wall -Wextra" -C tests test
